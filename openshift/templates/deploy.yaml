apiVersion: v1
kind: DeploymentConfig
metadata:
  name: mygovbc-splunk-forwarder
  namespace: gcpe-mygovbc-msp-dev
  selfLink: /oapi/v1/namespaces/gcpe-mygovbc-msp-dev/deploymentconfigs/mygovbc-splunk-forwarder
  uid: 4eb4e424-d912-11e6-89a5-0050568379a2
  resourceVersion: '33902668'
  generation: 11
  creationTimestamp: '2017-01-12T21:58:53Z'
  labels:
    app: mygovbc-splunk-forwarder
  annotations:
    openshift.io/generated-by: OpenShiftWebConsole
spec:
  strategy:
    type: Rolling
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 600
      maxUnavailable: 25%
      maxSurge: 25%
    resources:
  triggers:
    -
      type: ImageChange
      imageChangeParams:
        automatic: true
        containerNames:
          - mygovbc-splunk-forwarder
        from:
          kind: ImageStreamTag
          namespace: gcpe-mygovbc-msp-dev
          name: 'mygovbc-splunk-forwarder:latest'
        lastTriggeredImage: '172.50.0.2:5000/gcpe-mygovbc-msp-dev/mygovbc-splunk-forwarder:latest'
    -
      type: ConfigChange
  replicas: 1
  test: false
  selector:
    deploymentconfig: mygovbc-splunk-forwarder
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: mygovbc-splunk-forwarder
        deploymentconfig: mygovbc-splunk-forwarder
    spec:
      containers:
        -
          name: mygovbc-splunk-forwarder
          image: '172.50.0.2:5000/gcpe-mygovbc-msp-dev/mygovbc-splunk-forwarder:latest'
          ports:
            -
              containerPort: 8080
              protocol: TCP
          env:
            -
              name: TARGET_URL
              value: ''
            -
              name: TARGET_HEADER_HOST
              value: ''
            -
              name: TARGET_USERNAME_PASSWORD
              value: ''
          resources:
            requests:
              cpu: 100m
          terminationMessagePath: /dev/termination-log
          imagePullPolicy: Always
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext:
status:
  latestVersion: 8
  observedGeneration: 11
  replicas: 1
  updatedReplicas: 1
  availableReplicas: 1
  details:
    message: 'caused by an image change'
    causes:
      -
        type: ImageChange
        imageTrigger:
          from:
            kind: ImageStreamTag
            namespace: gcpe-mygovbc-msp-dev
            name: 'mygovbc-splunk-forwarder:latest'
